[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ipl"
version = "1.0.0"
description = "IPL Analytics 2008-2025"
authors = [{name="Shreyas Tikare"}]
requires-python = ">=3.14,<3.15"

[tool.setuptools.packages.find]
where = ["src"]
